---
deployment:
  tasks:
    - export DEPLOYPATH=/home/innovate/innovateaheadgroup.com/
    - /bin/mkdir -p "$DEPLOYPATH"
    - /usr/bin/rsync -a --delete \
        --exclude=".git" \
        --exclude=".well-known" \
        --exclude="cgi-bin" \
        ./ "$DEPLOYPATH"
